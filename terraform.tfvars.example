# Authentication (required).
tenancy_ocid         = "ocid1.tenancy.oc1..aaaaaaaa..."
user_ocid            = "ocid1.user.oc1..aaaaaaaa..."
fingerprint          = "xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx"
private_key_path     = "~/.oci/oci_api_key.pem"
private_key_password = "changeme"
region               = "us-ashburn-1"

# Compute (required). Path to the SSH public key authorized on the instance.
ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# Ampere A1 capacity varies by availability domain. If a launch reports
# "Out of host capacity", try 2 or 3 rather than changing the shape.
# availability_domain_number = 1

# Networking (optional, defaults shown). These match the values the OCI
# "VCN with Internet Connectivity" wizard prefills.
# name_prefix               = "free"
# vcn_cidr_blocks           = ["10.0.0.0/16"]
# vcn_dns_label             = "free"
# public_subnet_cidr_block  = "10.0.0.0/24"
# private_subnet_cidr_block = "10.0.1.0/24"
